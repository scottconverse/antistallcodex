# Changelog

## 0.1.0

- Initial AntiStallCodex release.
- Package Codex skill with `AntistallON` and `AntistallOFF` desktop invocation.
- Add global Codex `Stop` hook installer.
- Add helper commands for `on`, `off`, `status`, `done`, `blocked`, and `question`.
- Add loop guard support for Codex `stop_hook_active` plus fail-open block counter.
